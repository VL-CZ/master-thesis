\chapter{Introduction}

Reference documentation is widely used in software development to document project APIs, making the development process much easier.
It provides an overview of the types and members declared in the project, along with their documentation comments.
Reference documentation is typically generated by a program called a reference documentation generator (or simply a documentation generator).
Some programming languages include a standard reference documentation generator as part of their SDK (such as \texttt{Javadoc} for Java). 
In other languages, there's a well-established third-party generator, e.g. \texttt{Sphinx} for Python.
However, in .NET, the situation is quite different.
There are multiple generators available, including \texttt{DocFX}, \texttt{Doxygen}, and \texttt{SHFB}, each having its advantages and limitations, 
such as the need for extensive configuration or platform dependency.
This brings us to the primary goal of the thesis -- to develop a reference documentation generator for .NET.
The goals will be discussed in more detail in the following chapters.

\section{Context}

Before proceeding to the goals of the thesis, it's important to describe the use cases of reference documentation and its broader context.

The reference documentation is primarily used by these two types of users:
\begin{itemize}
    \item \textbf{Developers using the library} access the reference documentation to view the library's documented API. 
    Typically, only publicly visible types and members are included. 
    Users typically reach the reference documentation via links from the user documentation.
    \item \textbf{Developers maintaining the library} may also use the reference documentation, although this use case is slightly less common.
    Here, the primary goal of the documentation is to help the programmers understand the project's internal structure. 
    It is very beneficial during the process of onboarding new team members. 
    In this case, the documentation often includes non-public members as well.
\end{itemize}

The documentation may be created in many formats, such as HTML, Markdown, or LaTeX, with HTML being the most common.
It displays the documentation comments contained in the code, along with the type and member data, that the generator extracts from the source or intermediate code.

Figure~\ref{fig:ref-doc-example} shows an example reference documentation for the \texttt{User} class generated by \texttt{Doxygen}.
Notice that there's a dedicated page for the type containing its documentation \textit{(A)} as well as the list of the declared members with the corresponding documentation comments \textit{(B)}. 
There is also a menu with the search bar at the top of the page \textit{(C)}.
The same observations apply to the vast majority of reference documentation pages, regardless of the generator.

Reference documentation is often accompanied by user documentation, which typically consists of text and code snippets.
The user documentation can be written in HTML; however, there's an easier way to do it -- by using a static site generator (SSG).
SSGs, such as Jekyll or MkDocs, are programs that use text written in a markup language (often Markdown) to create static web pages.
% SSGs enable us to focus only on the textual content of the documentation, and no HTML or CSS knowledge is required, which is a great advantage.
When building the documentation, we typically choose one of the provided HTML templates to create the webpage.

Many reference documentation generators, for instance, DocFX have at least some of the capabilities of SSGs. 
Alternatively, it is also possible to use 
the reference documentation generator solely for the reference docs and
create the user documentation using an SSG.

\begin{figure}
\centering
\includegraphics[width=1\linewidth]{img/ref-doc-example.png}
\caption{Reference documentation generated by Doxygen}
\label{fig:ref-doc-example}
\end{figure}

\section{Goals}

We've gained a basic understanding of the context, and in this chapter, we'll present the main goals of our application.

First of all, many generators, including Doxygen and DocFX, require a non-trivial setup.
While the extensive amount of options may seem like an advantage, a nontrivial amount of time is required to become familiar with these tools.
Often, especially in the case of small libraries, the consequence is that the creator
decides not to generate the reference documentation, simply because he does not have the time or motivation to study the configuration. 

This leads us to our first goal -- the documentation generator should be easy to use.
In other words, simply running the generator from the command line and specifying the input should be sufficient, as in the case of \texttt{Javadoc}.
On the other hand, there should be support for using a configuration file, so the users don't need to repeat the same configuration next time.
However, the default options should be reasonable and applicable in most cases.
Thus, we can finally formulate the first goal:

\begin{itemize}
    \item \label{G1} G1: Zero-configuration build
\end{itemize}

Another observation is that users may not want to use the default UI. Instead, they could prefer a custom HTML template or the one provided by the community.
The reason may be a different design or color palette that better suits the project.
So, the second goal is:
\begin{itemize}
    \item G2: Support for custom templates
\end{itemize}

As mentioned before, it is often preferred to have user documentation alongside the reference one. 
We'd typically use an SSG for user docs; however, this is sometimes an unnecessarily complex solution, especially if we only need to add a few pages.
A good example is creating a custom \textit{index} or \textit{FAQ} page.
Our generator should by no means offer all of the features provided by SSGs; nevertheless, there should be at least basic support for custom HTML pages, which is our next goal.

\begin{itemize}
    \item G3: Support for custom static pages
\end{itemize}

The project's API typically evolves; however, not all developers use the most recent version.
As a result, we often need to maintain multiple versions of the documentation (each corresponding to a specific version of our library) at one time.
Documentation versioning can be achieved e.g. by using \textit{Read the Docs} hosting platform for documentation, as described in its official documentation~\cite{read_the_docs}.
Then, the documentation typically contains a dropdown menu that allows users to select a specific version.
However, the big disadvantage is the coupling to the specific platform.
Support for documentation versioning is one of our goals as well; also note that this is something that neither \texttt{DocFX}, \texttt{Doxygen}, or \texttt{SHFB} supports.

\begin{itemize}
    \item G4: Support for documentation versioning
\end{itemize}

We should also consider that the program may be written in any .NET language. 
Although C\# is the most popular, other .NET languages, such as F\# are also used in specific cases.
We don't want to exclude libraries written in those languages, so our app should be capable of handling programs written in any popular .NET language, which is our next goal.

(TODO, ma to zde byt?)
\begin{itemize}
    \item G5: Support for multiple .NET languages
\end{itemize}

Last but not least, we also want to follow the recent trends and provide a modern visually appealing user interface.
As we've seen in Figure~\ref{fig:ref-doc-example}, the default UI of Doxygen is quite outdated,
so this requirement cannot be assumed by default.
Thus, our last goal is:

\begin{itemize}
    \item G6: Modern UI
\end{itemize}

We've outlined the main goals of the application.
In the following chapters, we will analyze the problem in more detail.