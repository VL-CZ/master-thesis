\chapter{Architecture}

In this chapter, we will describe the architecture of our app.
The main project is divided into four folders:

\begin{itemize}
    \item \texttt{AssemblyAnalysis} -- contains types responsible for extracting the relevant data from an assembly.
    \item \texttt{CodeElements} -- contains objects representing the types and members, introduced in sections~\ref{sec:member-repr} and \ref{sec:type-repr}.
    \item \texttt{DocExtraction} -- contains types responsible for extracting the documentation from the XML documentation file and assigning them to the corresponding type or member.
    \item \texttt{TemplateProcessors} -- contains types responsible for generating the reference documentation from the type and member objects.
\end{itemize}

\section{Workflow}
The whole process is managed by \texttt{DocGenerator} classes, stored in the root directory.
Additionally, the project also contains a \texttt{Tools} folder, containing additional tools shared between those project parts.

The simplified process of generating documentation is displayed in Figure XX.
First, it is necessary to extract the information from an assembly and create the objects representing types and members, which is performed by the AssemblyTypeExtractor class.
These objects are then stored within the TypeRegistry class, which is further passed to the DocCommentExtractor.
This class extracts the documentation comments from the XML documentation file and assigns them to the corresponding types and members.
Thus, the TypeRegistry now contains types with documentation.
Next, this registry is passed to the ITemplateProcessor interface, which represents the base interface for processing various types of templates, as described in section XX.
The default implementation, called RazorTemplateGenerator uses Razor templates, and processes them with the provided data and thus creates the reference documentation.

\section{CodeElements}

This folder contains both type and member objects, introduced in sections~\ref{sec:member-repr} and \ref{sec:type-repr}.

\subsection{Members}

Member and their interfaces are stored in \textit{Concrete/Members} and \textit{Abstract/Members} subfolders.

First, there is a separate class for each kind of member, such as field, method, and property, as illustrated in Figure~\ref{fig:members-concrete}.

Thus, the classes are called FieldData, MethodData, PropertyData, etc.
These classes have suffix Data, as opposed to the types provided by reflection, whose names end with Info.

TODO: popsat strukturu

Also notice that there is a separate class for both operators (OperatorData) and indexers (IndexerData), inherited from MethodData, or PropertyData, respectively.
That is because an indexer in .NET is essentially a property, but with extra special functionality.

The common code is extracted to base class, called MemberData.
summary doc, remarks doc, name, ID string, etc.
Similarly, there is a MethodLikeMemberData class containing the code shared between methods and constructors.
Furthermore, there is a ParameterData class representing a parameter, outside the hierarchy.

\begin{figure}[H]
\centering
\includegraphics[width=1\linewidth]{img/diagram-members.png}
\caption{Template processors -- architecture}
\label{fig:members-concrete}
\end{figure}

The interfaces are shown in Figure XX. 
There is an interface for each concrete type -> IFieldData, IMethodData, IPropertyData, etc.

Notice that unlike the concrete member objects, the interfaces provide a read-only view over the data, as e.g. the documentation comments cannot be assigned.
TODO: popsat vyznam

Furthermore, there a several super-interfaces that represent common properties of a selection of members.

TODO: diagram

\subsection{Types}

Similarly to members, the objects representing types are stored inside the abstract/types and concrete/types folders.

Unlike the System.Type class, there is a hierarchy of types representing different kinds

\begin{itemize}
    \item EnumData
    \item DelegateData
    \item ObjectTypeData
\end{itemize}

TODO: popsat strukturu

Furthermore, there is a TypeNameData class representing a field type or a return type.

Similarly, there are interfaces 

\subsection{Type registry}

All types are stored and passed in a type registry class, which contains all types,

\subsection{Tools}

\section{Assembly Analysis}

This folder contains classes responsible for creating and populating the TypeRegistry with types extracted from an assembly.

Several classes: AssemblyTypeExtractor is the most important, as it extracts the data from an assembly in method XX.
There is a method for creating enum and delegate types.

For classes, interfaces and structs, the situation is quite different.
The construction of those is much more complicated, as they may contain various members.
For that reason, we used builder pattern, and created ObjectTypeBuilder class.
Moreover, each member object is created from the corresponding reflection type. 
This is done by creator classes, that are present for each member.
-> FieldDataCreator, MethodDataCreator, etc.

\section{Documentation Extraction}

\section{Template Generators}
\subsection{Template Model}
\subsection{Template Model Creators}
\subsection{Templates}

\section{Logging}
Describe what happens if there are errors.

\section{Tests}
Describe the packages used (xUnit, FluentAssertions, NSubstitute) and include snippets.

\subsection{Unit Tests}
\subsection{Integration Tests}

\section{Code Quality}

\subsection{.NET Analyzers}
\begin{itemize}
    \item Describe .NET analyzers.
\end{itemize}

\subsection{Code Style}
\begin{itemize}
    \item Describe code style.
\end{itemize}

\subsection{CI/CD}
\begin{itemize}
    \item Describe GitHub Actions.
\end{itemize}

\subsection{Workflow}
\begin{itemize}
    \item Feature branches, tasks, merge with linear history, etc.
\end{itemize}

\section{Extensibility}

\subsection{Custom Razor Templates}
Step-by-step guide.

\subsection{Custom Template Generator}
How to create a custom non-Razor template.

\chapter{User Documentation}

\section{How to Use}
Include screenshots of the result.

\section{Configuration}
\subsection{Option 1}
\subsection{Option 2}

\section{How to Make a Custom Razor Template}
